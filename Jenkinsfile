pipeline {
    agent any
    stages {
        stage("install dependencies") {
            steps {
                script{
                    sh 'python --version'
                }
            }
        }
    }
}
